<!DOCTYPE html>
<html lang="nl-BE">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokémon Details</title>
    <link rel="stylesheet" href="/css/test.css">
    <link rel="stylesheet" href="/css/reset.css">
</head>
<body>

<section class="pokemonDetail">
    <article class="pokemonCardDetail">
        <img :src="officialArtworkUrl" alt="<%= pokemon.name %>" class="pokemonImageDetail">
        <div class="pokemonInfoDetail">
            <h2 class="pokemonNameDetail">
                <%= pokemon.name %> (#<%= pokemon.id %>)
            </h2>
            <p class="pokemonTypeDetail">Type: <%= pokemon.types.map(type => type.type.name).join(', ') %></p>
            <div class="pokemonButtons">
                <button class="catchButton">Catch!</button>
                <button class="compareButton">Compare with...</button>
            </div>
        </div>
    </article>

    <section class="evolutionChain">
        <% if (pokemon.evolution && pokemon.evolution.chain) { %>
            <% let current = pokemon.evolution.chain; %>
            <% while (current) { %>
                <article class="evolutionStage">
                    <img :src="current.sprites.front_default" alt="<%= current.name %>" class="pokemonImageDetail">
                    <p class="evolutionName"><%= current.species.name %></p>
                </article>
                <% current = current.evolves_to[0]; %>
            <% } %>
        <% } else { %>
            <p>Evolution chain not available.</p>
        <% } %>
    </section>
</section>

<script src="/js/carddetails.js"></script>
</body>
</html>